I'm not really suggest you run btsync behind proxy, the speed seems very slow.

Since access btsync webui is not necessary after setup user and password, just run it as a standlone container will be fine.
